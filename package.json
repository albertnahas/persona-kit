{
  "name": "personakit-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter next-starter dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.2",
    "release-it": "^19.0.6",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
